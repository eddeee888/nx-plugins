{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "NxGraphqlCodeGenerator",
  "title": "",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "alias": "p",
      "$default": {
        "$source": "projectName"
      },
      "x-prompt": "What project do you want to add config to?"
    },
    "schema": {
      "type": "string",
      "description": "URL to GraphQL endpoint, local path/glob patterns to .graphql file/s or JavaScript file that exports the schema",
      "alias": "s",
      "x-prompt": "Where is your GraphQL schema?"
    },
    "documents": {
      "type": "string",
      "description": "Array of paths or glob patterns for files which export GraphQL documents e.g. ./src/**/*.graphql",
      "alias": "d",
      "x-prompt": "Where are your GraphQL documents?"
    }
  },
  "required": ["project"]
}
