{
  "version": 2,
  "outputCapture": "direct-nodejs",
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "Run GraphQL codegen",
  "description": "",
  "type": "object",
  "properties": {
    "configFile": {
      "type": "string",
      "description": "Path to codegen config file"
    },
    "watch": {
      "type": "boolean",
      "description": "Run in watch mode if true"
    },
    "verbose": {
      "type": "boolean",
      "description": "Run in verbose mode if true"
    }
  },
  "required": ["configFile"]
}
